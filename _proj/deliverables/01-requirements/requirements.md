> **Version:** v001.00
> **Last updated:** YYYY-MM-DD
> **Source of truth** for Execution phase. History: [archive/](archive/)

# Requirements

<!-- TREQ Format Reference
  Core fields (required on every TREQ):
    Title, Priority, Status, Source, Relationship, Deliverable, Rationale

  Conditional fields (added when applicable):
    Supersedes      — when this TREQ replaces a prior task-level requirement
    Extended by     — annotation added to an existing TREQ when a new TREQ declares EXTENDS
    Replaced by     — annotation added when a new TREQ declares REPLACES (original → DEPRECATED)
    Deprecated on   — date of deprecation (accompanies Replaced by)
    Implemented by  — BL-### reference, added alongside Replaced by only when the
                      original TREQ was previously implemented (check _backlog_completed.md)

  Status values: DRAFT → LOCKED → MIGRATED → DEPRECATED
  Priority values: P0 (critical) | P1 (important) | P2 (medium/nice-to-have) | REJECT
  Relationship values: NEW | EXTENDS TREQ-### | REFINES TREQ-### | REPLACES TREQ-###
-->

## TREQ-001
- **Title:** [Concise statement of what must be true]
- **Priority:** [P0 / P1 / P2]
- **Status:** DRAFT
- **Source:** [DD-### or comma-separated DDs]
- **Relationship:** [NEW / EXTENDS TREQ-### / REFINES TREQ-### / REPLACES TREQ-###]
- **Deliverable:** [Type → agent → output path]
- **Rationale:** [Why this requirement exists, what problem it solves]
